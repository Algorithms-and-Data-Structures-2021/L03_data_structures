#include <iostream>

#include <deque>
#include <list>
#include <queue>
#include <stack>
#include <vector>

using namespace std;

int main(int argc, char **argv) {
  std::cout << "Hello, stranger!" << std::endl;

  auto array_list = vector<int>();
  auto linked_list = list<int>();
  auto cpp_queue = queue<int>();
  auto cpp_deque = deque<int>();
  auto cpp_stack = stack<int>();

  return 0;
}